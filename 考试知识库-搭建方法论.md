# 考试知识库搭建方法论 v3

> **版本**：v3（2026-08-09）；v3.1（2026-08-13）新增 §六「检索工具与 dsh 交接」
> **适用范围**：任何考试（CIA 科目一/二/三、CISA、CPA 等）的知识库搭建。一场考试一个顶层目录（如 `考试知识库/CIA-Part3/`），直接套本文档全流程。
> **核心定位**：知识库是给 Claude 和 dsh（DeepSeek Harness）共用的"考试记忆体"，不是给人通读的笔记。用户靠刷题备考，不读知识库。
> **前置产出**：「审计知识库-构建方法论」（作者私有笔记，不随本仓库分发） 是多 Agent Workflow 的审计版；本文档是考试版，可对照阅读。

## 📖 本文档怎么用（给 AI 读，必看）

- 本文档是**流程手册**：按章节顺序执行（〇前置 → 一摄入 → 二建目录 → 三精讲 → 四错题 → 五检索 → 六工具），不要跳步
- 新考生建档**先读仓库根目录 `START-HERE.md`**（收集考试/教材/路径三要素），再回到本文档跑流程
- 文中所有 `%USERPROFILE%`、`C:\Users\...`、`AppData/Local/Temp/...` 等路径都是**作者实测环境的示例**——执行时一律替换为考生机器的真实路径，路径不存在就先问考生
- 文中提到的脚本（`batch_ocr.py`、`chandra_ocr.py` 等）是作者本机的可选工具，**不是本项目内容**；考生没有就用任何等效工具（PaddleOCR、pymupdf、在线 OCR 等）完成同一目标
- 本文档通用流程与 CIA 无关；`CIA-Part3/` 目录只是**示例实例**，里面的章节名/教材名（Gleim、兆泰等）只作参考

---

## 〇、前置条件（不满足别开工）

> ⚠️ 下表是**作者实测环境**（Windows 11 + Python 3.11 + Claude Code），仅供参考。考生的机器只需满足：**能跑 Python 3 的电脑 + 任意 AI 客户端（Claude Code / dsh / 其他）+ 足够对话额度**。具体路径按考生实际情况替换。

| 项 | 要求（作者实测环境） | 说明 |
|----|------|------|
| AI 客户端 | `"defaultMode":"dontAsk"` + `"skipWorkflowUsageWarning":true` + 全工具 allow | 用户级 `%USERPROFILE%\.claude\settings.json`，改完必须重启 Claude；用其他客户端则跳过 |
| 启动方式 | `claude --dangerously-skip-permissions` | 否则多 Agent 每步都要点确认（仅 Claude Code） |
| API 余额 | ≥ 500 万 tokens | 每章精讲约 40-60K tokens，A 块 20 章 + B 块约 1.2M+；余额不足 Agent 2/3 审查会 402 |
| Python | 3.11.9（示例） | 跑 OCR 脚本**必须**加 `PYTHONIOENCODING=utf-8` |
| 目录 | 一场考试一个顶层目录，如 `考试知识库/CIA-Part3/` | 结构见 §二 |

---

## 一、PDF 摄入（从"转 PDF"这一步开始）

> 目标：把教材 PDF（电子版或扫描件）变成干净的 Markdown，进 `教材原文/`。

### 1.1 决策树（先判断，不盲目 OCR）

```
输入是 PDF
│
├─ 1️⃣ 先测文本层：pymupdf get_text()
│   ├─ 有文字（电子版 PDF）→ 直接用文本层，【不 OCR】✓
│   └─ 无文字（扫描件）→ 走 OCR ↓
│
├─ 2️⃣ 按文档类型选：
│   ├─ 📕 整本/批量扫描教材 → RapidOCR batch_ocr.py（多进程，主选）
│   ├─ 📄 单章/几页扫描件   → RapidOCR ocr_pipeline.py --start/--end
│   ├─ 🩹 关键页语义纠错     → Chandra OCR 2 --max-dim 1800（慢，只补错页）
│   └─ ❌ WPS"可搜索"PDF    → 弃用（文本层质量差，IT 术语识别崩坏）
```

### 1.2 命令速查

```bash
# 测文本层（不 OCR）
PYTHONIOENCODING=utf-8 python -c "import fitz; d=fitz.open('x.pdf'); print(d[0].get_text()[:200])"

# RapidOCR 批量（整本教材，主选）
PYTHONIOENCODING=utf-8 python "%USERPROFILE%/AppData/Local/Temp/batch_ocr.py" \
  --dir "%USERPROFILE%/Desktop/CISA/CIA" \
  --out "%USERPROFILE%/AppData/Local/Temp/ocr_out/<书名>" \
  --dpi 150 --workers 4 --threads 4
# 文件名匹配：两位数字开头且后接非数字（如 01治理.pdf）；自动跳过 WPS 产物

# RapidOCR 单章/单文件
PYTHONIOENCODING=utf-8 python "%USERPROFILE%/AppData/Local/Temp/ocr_pipeline.py" 文件.pdf -o ./ocr_out --dpi 150 --tag raw
# --tag raw：输出 名字_raw.md（多任务防覆盖）；指定页范围：--start 5 --end 10

# 清洗（去页眉页脚/版权/修复符号）
PYTHONIOENCODING=utf-8 python "%USERPROFILE%/AppData/Local/Temp/batch_clean.py" --dir ./ocr_out
# 单个：PYTHONIOENCODING=utf-8 python "%USERPROFILE%/AppData/Local/Temp/clean_md.py" 输入_raw.md 输出_clean.md

# Chandra OCR 2（关键页语义补错，慢）
PYTHONIOENCODING=utf-8 python "%USERPROFILE%/Documents/ClaudeCode/chandra-ocr/chandra_ocr.py" 教材扫描件.pdf --max-dim 1800
```

### 1.3 OCR 坑（必读）

1. **onnxruntime WinError 1114**：已修复（14.50 VC 运行时 DLL 已放 Python 根目录/DLLs），**别再动 DLL**。
2. **Chandra 密集页必须 `--max-dim`**：默认 900 对密集中文页"字太小"→ 返回空字符串（不是报错），易误判为"识别完毕"。Gleim 622 页用 RapidOCR 66 分钟跑完，Chandra 只用于补错页。
3. **`batch_ocr.py` 只认两位数开头的章节文件**：文件名必须是 `01治理.pdf` 这种格式才会被处理。长任务/多文件用 `ocr_split.py` 按页拆段多进程。
4. **后台任务被会话中断会残留孤儿 worker**：重启前先 `tasklist | grep python` 清残留，否则僵尸进程占 CPU 且锁文件。

---

## 二、清洗后入库：目录结构

> 通用目录口径见仓库根 `START-HERE.md`「知识库目录结构」一节。以下展示的是 **CIA Part 3 实例**（含 IPPF/Gleim/兆泰 等实例文件），结构可复制、文件名按你的考试替换。

```text
考试知识库/
└── CIA-Part3/                        ← 一场考试一个顶层目录
    ├── _索引.md                      ← ⭐三级定位引擎（考试→板块→章→节→知识点→文件路径）
    ├── 检索协议.md                    ← ⭐给 Claude 的操作协议（做题/入库/输出格式）
    ├── IPPF索引.md                    ← IPPF 条款号→正文行锚点（治理/风险/职业道德类题锚点层）
    ├── 思维导图.md                    ← Mermaid mindmap 全貌 + 折叠大纲（人工复习用）
    ├── 教材原文/                      ← 基础层，OCR 产物，一章一文件
    │   ├── 01-治理.md
    │   ├── 02-风险管理.md
    │   ├── …
    │   ├── IPPF官方版2017_clean.md    ← 官方准则原文（纯条文，抠措辞以它为准）
    │   ├── IPPF红皮书_clean.md        ← 主题式解读（定义/CAE/内审师/作用/程序）
    │   ├── 兆泰知识要素_clean.md      ← 按官方大纲四大板块组织
    │   └── 应试指南_clean.md          ← 按大纲四章组织，每章含典型模拟试题
    ├── 精讲笔记/                      ← ⭐精讲层，检索优先用它，笔记没有再翻原文
    │   ├── NN-章节名.md               ← A 块：业务知识精讲（Gleim 主干+融合源）
    │   ├── B-0X 文件名.md             ← B 块：IPPF 条款精讲（官方版+红皮书双源）
    │   └── _README.md                 ← 状态追踪（哪章完成/考点数）
    └── 错题库/                        ← 刷题闭环
        ├── 错题记录.md                 ← ⭐全部错题（单文件，每条 `## 错题 YYYY-MM-DD-NNN`）
        ├── _汇总.md                   ← 薄弱点统计（按章节/板块累计，错题入库立即更新）
        └── _README.md
```

**教材清册原则**：主教材（如 Gleim）按章拆文件；辅助教材（如兆泰/应试指南/IPPF）若本身就是章节化组织，可整本一文件（靠大纲板块行号锚点定位）。多套教材各司其职——主教材做章骨架，融合源补考点厚度，官方原文抠措辞。

---

## 三、建索引（三级定位引擎）

> 核心：**先查 `_索引.md` 锁章节 → 只 Grep 单文件**，禁用全目录扫描（素材越多越关键）。

### 3.1 `_索引.md` 必须包含

1. **定位锚点（两类）**：
   - **治理/风险/职业道德类** → 锚 IPPF 标准条文（如 2110、2110.A1）→ 先查 [[IPPF索引]] → 跳 B 块精讲 → 精讲未覆盖再翻红皮书/官方版原文
   - **业务知识类**（IT/财务/管理会计/法律经济）→ 锚 大纲板块 → 章 → 节 → 知识点 → 直接定位章节
2. **大纲板块 → 章节映射表**：板块（I.治理/II.风险管理/III.组织结构/IV.沟通/V.管理领导/VI.IT/Ⅶ.财务管理/Ⅷ.全球商业环境）+ 权重 + Gleim 章节 + 教材文件链接。**权重高板块（IT/财务）标 ⭐必看**。
3. **辅助教材大纲板块定位表**：兆泰/应试指南正文按官方大纲组织，给出板块→正文行号锚点（如 `兆泰知识要素_clean.md:L239`），题目按"大纲板块"表述时跳这里。
4. **章节知识点索引（三级）**：板块 → 章 → 节 → 知识点锚点。例：`[[01-治理]]：1.1 治理概述 · 1.2 内部审计师在治理过程中的作用 · …`

### 3.2 `IPPF索引.md`（治理/风险/职业道德类锚点层）

> ⚠️ **仅适用于 IIA 体系类考试**（CIA 等）。其他体系按各自的官方标准建锚点层：如 CISA 用 **ISACA ITAF/ISACA 标准**（不是 IPPF）；没有官方标准的考试可省略此层。

条款号 → 正文行锚点，双源：
- **官方版 2017**（`IPPF官方版2017_clean.md`）：条款原文措辞以此为准
- **红皮书**（`IPPF红皮书_clean.md`）：解读/释义

### 3.3 `检索协议.md`（给 Claude 的操作协议）

固化以下纪律（逐条写死，Claude 才能跨窗口一致执行）：
1. **题型板块判断**：40% IT 类→VI 板块；治理/风险/职业道德→I/II + IPPF；计算题→VII
2. **先查 _索引锁章节**，绝不直接全目录扫描
3. **只 Grep 单文件**（`教材原文/NN-章节名.md`），禁用全目录扫
4. **并行读取**：已知节编号可直接 offset 跳读
5. **做题默认完整四段式**：考点定位 → 准则/大纲依据 → 思路拆解 → 干扰项解析。**答案先出**（2026-08-02 定稿：紧凑版废弃，入库/汇总等收尾排在答案之后，不得拖延答案输出）
6. **本地教材兜底优先**：Gleim 未命中 → 按大纲板块切兆泰/应试指南 → 本地有依据就下结论
7. **外部搜索设上限**：仅当本地无法定论才外部搜，**限 2 轮**，无果即标注"待确认"
8. **错题入库（强制约定）**：用户发题即错题 → 答案讲解先行 → 入库（追加 `错题库/错题记录.md`）+ 立即更新 `_汇总.md`
9. **边界**：只答考纲内内容，考纲外明说"不在考纲范围"；OCR 原文错字按常识纠正后标注；优先精讲笔记，笔记没有再翻原文

---

## 四、分批精讲（核心杠杆）

> **精讲笔记是最大杠杆**：把"动态搜原文"变成"静态查表"（考点→结论→原文位置）。先做精讲，而非堆原文。
> ⚠️ 本节写作流程（writer + content + anchor 三 Agent、journal.jsonl 等）是作者在 **Claude Code** 中的实现示例；在 dsh 等其他客户端可用等价的多 Agent 工作流（如 dsh 的 workflow 工具）完成同一目标——**流程是通用的，工具不是**。
> 用户已批准 A 块（业务知识）+ B 块（IPPF 条款）全做。每批 2-3 章，不要一次 10+。

### 4.1 A 块：业务知识精讲（`精讲笔记/NN-章节名.md`）

- **主骨架**：Gleim 一章一文件（正文有效范围须先侦察，尾部常混入大纲交叉索引/书目/报表范例）
- **融合源**：兆泰/应试指南按考点补厚度（融合内容独立成字段，不进核心结论）
- **每章** = 1 写作 Agent（读 Gleim 全文 + 融合源）+ 2 审查 Agent（内容审查 + 锚点校验），40-60K tokens
- **考点格式**：大纲位置 / 准则依据 / 核心结论 / 逻辑拆解 / 考法 / 易错点 / 融合补充 / 锚点（代表行，点号分隔）/ 关联考点
- **P/U 熟练水平标注**：以教材**尾部大纲交叉索引**为准，不能凭主循环侦察猜测

### 4.2 B 块：IPPF 条款精讲（`精讲笔记/B-0X 文件名.md`）

- **每个 IPPF 条款 = 1 考点**，实施标准（.A/.C）并入对应主条款
- **锚点双源**：官方版原文行 + 红皮书解读行，点号分隔；条款原文措辞以官方版为准，红皮书为解读
- **高频标注**：IPPF索引 中的 ⭐ 条款
- **格式**：条款地图（表格：条款/名称/要点一句话/正文考点）→ 红皮书区段导览（表格：考点/区段/行号范围）→ 逐考点（标准位置/官方原文/核心结论/逻辑拆解/考法/易错点/融合补充/锚点/关联考点）
- **融合遵循**：官方原文措辞以官方版为准，红皮书增量（审计立项、审计通知书等）一律放「融合补充」字段

### 4.3 锚点五类偏移（贯穿 A+B 全程，最普遍的坑）

写作 Agent 的锚点必须**逐个 Read 验证**，且**考点末尾汇总锚点块 与 核心结论内嵌锚点 都要验**。锚点偏移集中在：

1. **OCR 空列表标记行**（`a.` `1)` `d.原`）——锚到实际内容行，不锚标记行
2. **小节标题 / 引导行**（如"内部评估必须包括："）——锚到其后的内容行，部分标题后隔一行才是正文（OCR 噪声行须再跳行）
3. **表格标题 / 表格行标签 / 示例数据块标签**（如"周转率""本年度毛利率："）——锚到具体数据行
4. **OCR 分页残字** / 图表片段（如"产出。"）
5. **标题行独立成行**（官方版"1100一独立性与客观性"）——锚内容行

### 4.4 融合断言锚点铁律

- **每条融合断言都须锚点**（无源断言不得出现，改"无增量"指向或删）
- **多源归因的融合断言，每个来源都要有锚点**（15.4a 教训：归因兆泰+应试指南却只锚应试指南）
- **归错源**：融合断言源归属须 grep 全库核实（18.4c 教训：资源动因示例实际出自应试指南却归兆泰）
- **跨多行子断言每个都要锚**（同一 bullet 后半句易漏锚；20.4a 跨行子断言单独锚）
- **融合源列表只锚条目行，不锚引导行**；清单全量列举或注明截取，不许写"等八项"却只列 6 项

### 4.5 内容审查重点（content Agent 维度清单）

1. **P/U 水平核对**（以交叉索引为准，11.2 教训：主循环误判 P，writer 对照交叉索引纠正）
2. **禁止 IPPF 生拉**（业务知识板块严禁引 IPPF 条款，03 教训）
3. **清单完整性**（编号小节/清单整段没写，05 教训：挑战者战略组合/出版物渠道/CVA 攻击目标）
4. **融合材料占错字段**（09 教训：兆泰内容写进核心结论；应重构为"融合考点"并标正式归属）
5. **融合条目堆砌**（与考点无关的融合内容删掉）
6. **易错点缺失**（每个考点 section 都必须含四要素；writer 格式自检不可靠，主循环逐考点验证）
7. **易错点无原文依据 / 方向性错误**（17.6d 低税单元方向与本节示例矛盾）
8. **定义冲突加 ⚠**（14.1a 主要使用者口径差异；11.4 融合源与主干定义相反，以 Gleim 为准）
9. **过强绝对表述**（14.3a"仅用于税收目的"过强，改温和表述）
10. **融合补充/易错点重复**（2030 与 2070 逐字重复 → 只留指针）
11. **"（治理层）"无原文依据 + "始终"绝对化**
12. **引用措辞与锚点行微错位**（2130 控制定义用 2110 措辞却锚 2130 行）
13. **大纲字母码映射缺失/不统一**（19.2 小节内 B 码标注混乱）
14. **高频标注不一致+考点最薄**（脚本声明的高频考点全标「（高频）」且写够厚度）
15. **数字标签与条数一致**（"三环节"却列 4 项）
16. **官方原文字段被压缩截断**（须补齐或标注摘要）
17. **例外条款适用场景核对**（2240 口头同意例外仅限调整报批场景，不绑定初始批准）
18. **wiki-link 文件名与实际文件一致**（B-06 写 `[[B-07 工作标准2300系列]]` 而实际为 `2300-2600系列`；2026-08-09 抽查又抓出 **17 处断链三种形态**：①B 块互链旧名——`[[B-02 属性标准1100-1130]]` 实为 1000-1130、`[[B-03 属性标准1200-1300]]` 实为 1200-1322；②章内小节名误当文件名——`[[06-竞争战略]]`/`[[10-领导风格]]`/`[[10-团队建设]]`，实为章文件 `06-行业内的结构分析`/`10-领导与冲突管理`；③错章号——`[[07-财务管理]]` 实为 `17-管理会计I`。**写完必须跑脚本核对全部 `[[wiki-link]]` 目标 vs 实际文件名，勿凭记忆**）
19. **writer 编造引用**（不存在的"详见下方注"）
20. **"自称核实"不可信**（writer 称"锚点全部 Read 核实"仍被 anchor 抓 33-37 处——第三方验证不可省）

### 4.6 双 Agent 审查机制（write + content审查 + anchor校验）

```text
写作 Agent ──写笔记──> 精讲笔记/NN-章节.md
    │
    ├──> Agent A（内容审查）：事实/清单/P-U/归因/易错点/格式（见 4.5 清单）
    ├──> Agent B（锚点校验）：逐锚点核对行号，报 high/low 偏移（见 4.3 类型学）
    │    （A、B 并行）
    ↓
主循环：逐条核实 findings，真偏移才改，误报记录不盲从
```

**铁律**：
- **主循环逐行核实是最终防线**——content 会漏、anchor 也会误报（B-04 2060 误报 L1483、批7 13章 L190/L937 误报），双审查非万无一失
- **锚点块要整块校验**，勿只改报错那条（主循环自己也犯过：1111 锚点块误引 L738 实为 1112 标题行）
- **改锚点防重复**：引导行改内容行时该行可能已存在锚点（2120 L1045 改 L1049）
- **Workflow 返回映射陷阱**：`writer:r[0]` 常拿到审查结果而非写作 Agent 摘要，**须从 journal.jsonl 读全量**才能拿到写作摘要
- **多文件 awk 行号必须用 FNR 不能用 NR**（累计行号陷阱）

---

## 五、错题库（静态入库，无复习调度）

> 用户已确认：错题库**保持静态**，不引入间隔重复、不做随机组卷/AI 变体题、不做到期提醒。

### 5.1 约定（用户定稿）

- 用户发的**每一道题 = 错题**（"凡是我发的题，都说明我不会"）
- **答案讲解先行**，入库排在答案输出之后（不能因入库拖延用户看答案）
- 入库 = 追加到 `错题库/错题记录.md`（**单文件**，新题在文件顶部倒序），每条一个 `## 错题 YYYY-MM-DD-NNN` section
- **每题入库立即更新 `错题库/_汇总.md`**（按章节/板块累计，薄弱点统计）

### 5.2 错题条目格式

```markdown
## 错题 YYYY-MM-DD-NNN — 主题
> 题号 xx/99，单选题。（发题即错题约定）

- **题目**：…（含选项）
- **正确答案：X**
- **错因分类**：概念混淆 / 准则不熟 / 计算失误 / 审题失误
- **知识点ID**：VI-11.3 身份认证与加密（IPPF 类用 `IPPF-2110.A1 治理`）
- **复盘**：考点解析 + 教材原文依据（`文件:行`）+ ⭐陷阱 + 关联错题（编号）
```

### 5.3 `_汇总.md` 结构

- **章节表**：知识点ID | 章节文件 | 错题数 | 主要错因
- **板块表**：板块 | 错题数 | 占比（薄弱点=错题最多的板块）
- **薄弱点段落**：按板块分析
- **明细列表**：wiki-link 到具体错题
- **更新日志**：日期 + 累计题数

---

## 六、检索工具与 dsh 交接（2026-08-13 新增）

> 定位：知识库搭好后，Claude 和 dsh 都通过统一命令 `kb-search` 检索教材，替代"全目录 Grep"。
> 新科目只要建进 `考试知识库/<科目>/`，`kb-search` 自动覆盖，**零配置**。

### 6.1 全局命令 `kb-search`

- **脚本**：`C:\Users\Public\kb-search\kb-search.py`（⚠️ 纯 ASCII 路径，原因见 6.3）；shim 三份（bash/cmd/ps1）在 `AppData/Roaming/npm/`
- **范围**：递归搜整个 `考试知识库/`（含未来新科目），教材原文 + 精讲笔记 + 错题库 + 索引，UTF-8 输出
- **常用**：
  ```bash
  kb-search "身份认证"                             # 全文搜，按命中数排序，每处带 行号+片段
  kb-search --locate "COSO"                        # 只搜 _索引/思维导图/IPPF索引，定位「板块→章→节」
  kb-search --view "教材原文/11-IT安全性和应用程序开发.md" 120-140   # 看某文件某段原文
  kb-search --info   /   --list                    # 知识库概览 / 文件清单
  ```
- **选项**：`--dir 教材原文|精讲笔记|错题库`、`--project CIA-Part3`、`--max-files N`、`--max-hits N`、`--context N`
- 与检索纪律的关系：`--locate` 对应纪律 2（先 _索引 锁章节）；`--view` 对应纪律 4（已知节编号直接跳读）；不改变"精讲优先、原文兜底"

### 6.2 dsh 侧 skill `exam-kb-search`

- **位置**：`~/.agents/skills/exam-kb-search/SKILL.md`（dsh 自动扫描；如需给 Claude 用，加 symlink 到 `~/.claude/skills`）
- **内容**：知识库位置/结构、检索纪律、做题四段式、发题即错题入库约定（与 §3.3/§5.1 一致）
- `~/.dsh/AGENTS.md` 已加「考试知识库」检索说明，dsh 每会话自动读
- **实测**：dshq 确认 skill 可见、`kb-search --info`/`--locate` 正常、中文查询无误

### 6.3 ⚠️ 工程坑：中文用户名 + PowerShell 5.1（与 koffi 编译同根因）

- **现象**：脚本路径含 `%USERPROFILE%\...` 时，Windows PowerShell 5.1 原生参数传递把中文乱码（`中文用户名`），Python 打不开脚本；bash/cmd 正常
- **对策**：脚本放纯 ASCII 路径（`C:\Users\Public\kb-search\kb-search.py`）；ps1 shim 的中文查询改走环境变量 `KB_QUERY`（Windows env 是 UTF-16 安全，绕开原生 argv 编码）
- **规则沉淀**：给 dsh/跨 shell 用的脚本一律放 ASCII 路径；ps1 传中文参数用环境变量兜底

---

## 七、验收 checklist（每批结束自查）

- [ ] 每章精讲文件存在，考点数符合侦察（A 块章节数 / B 块条款数）
- [ ] 每个考点 section 含四要素：核心结论 / 逻辑拆解 / 考法 / 易错点
- [ ] 核心结论内嵌锚点 + 考点末尾汇总锚点块**都已 Read 核实**，无标题行/引导行/表格标签/OCR 空标记
- [ ] 融合断言每条有锚点，多源归因每源有锚，无归错源
- [ ] P/U 水平与教材尾部交叉索引一致
- [ ] 业务知识板块无 IPPF 生拉；定义冲突有 ⚠；无过强绝对表述
- [ ] 高频考点全标「（高频）」且写够厚度
- [ ] 大纲字母码唯一映射到考点
- [ ] wiki-link 文件名与实际一致
- [ ] `_索引.md` 同步更新该章考点/锚点
- [ ] 错题入库时 `_汇总.md` 已同步（当日题）
- [ ] 进度汇报：文件/考点数/findings 数/修正情况

---

## 八、常见坑 + 对策速查表（13 批教训收敛）

### 8.1 检索/做题期

| 坑 | 对策 |
|----|------|
| 全目录 Grep 噪音大、拖慢定位 | 先查 _索引 锁章节，只 Grep 单文件 |
| 紧凑讲解"难一次理解到位" | 默认完整四段式，答案先出 |
| 思路拆解层层铺陈 | 抓核心因果链，精炼表达 |
| 外部搜索死磕（3/100 COBIT 题耗时最长） | 限 2 轮，本地兆泰能定就不外搜 |
| 入库拖延答案输出 | 入库/汇总排在答案之后 |

### 8.2 OCR/摄入期

| 坑 | 对策 |
|----|------|
| 无文本层判断 | 先 pymupdf get_text，有文字不 OCR |
| Chandra 密集页返回空 | 必须 `--max-dim 1800` |
| WPS 可搜索 PDF | 弃用 |
| 后台中断残留孤儿 worker | 重启前 `tasklist \| grep python` 清残留 |
| 中文乱码 | 全程 `PYTHONIOENCODING=utf-8` |

### 8.3 精讲期（锚点类）

| 坑 | 对策 |
|----|------|
| 锚点偏移（5 类，见 4.3） | writer 逐个 Read 验证 + anchor 第三方校验 + 主循环逐行核实 |
| 只验内嵌锚点、漏汇总锚点块 | 两处都验，锚点块整块校验 |
| writer"自称核实" | 不信自述，靠 anchor Agent + 主循环 |
| 锚点误报 | 主循环逐行核实原文，真偏移才改 |
| 锚点重复 | 改锚点前查该行是否已被锚 |

### 8.4 精讲期（内容类）

| 坑 | 对策 |
|----|------|
| P/U 误判 | 以教材尾部交叉索引为准 |
| 业务板块引 IPPF | 严禁，改"无" |
| 融合材料占主字段/易错点 | 独立「融合补充」字段，标正式归属 |
| 融合断言无锚/多源只锚一源/归错源 | 每条断言有锚，每源有锚，grep 核实来源 |
| 易错点缺失/无依据/方向错 | 四要素必查，易错点须有原文依据 |
| 清单漏项写"等八项" | 全量列举或注明截取 |
| 绝对化表述 | 改温和，附依据 |
| 官方原文被截断 | 补齐或标摘要 |
| wiki-link 文件名不符 | 与实际文件名核对，三种形态全查：①B 块互链写旧名（`1100-1130`/`1200-1300`）②章内小节名当文件名（`[[10-领导风格]]`）③错章号（`[[07-财务管理]]`→17）；写完统一 grep `\[\[.*\]\]` 对照目录文件名 |

### 8.5 工程/流程类

| 坑 | 对策 |
|----|------|
| `writer:r[0]` 拿到审查结果 | 从 journal.jsonl 读全量拿写作摘要 |
| awk 多文件 NR 陷阱 | 用 FNR |
| 402 余额不足审查失败 | 余额恢复后重跑；Agent 1 笔记仍会生成 |
| 每批并行太多烧光余额 | 2-3 章/批，不一次 10+ |
| 权限弹窗 | settings.json `defaultMode:dontAsk`（用户级）+ 重启 |
| Workflow 确认弹窗不生效 | 一次性启动所有底稿，批量确认 |
| wiki-link 断链残留（2026-08-09 抓 17 处） | 批次收尾脚本核验：`grep -oh '\[\[[^]]*\]\]' 精讲笔记/*.md` 去前缀 → 与 `ls *.md` 文件名 `comm -23`，差集为空才算过 |
| 脚本路径含中文用户名 → PowerShell 5.1 乱码（`中文用户名`） | 脚本放纯 ASCII 路径（`C:\Users\Public\...`）；ps1 传中文参数走环境变量（Windows env 是 UTF-16 安全），见 §6.3 |

---

## 九、新科目启动清单（Copy-Paste）

1. **建目录**：`考试知识库/<科目>/` + `教材原文/` + `精讲笔记/` + `错题库/`
2. **收教材**：确认每本 PDF 是否有文本层；扫描件走 §1 OCR
3. **OCR + 清洗**：按 §1，产物进 `教材原文/`
4. **建 `_索引.md`**：大纲板块→章节映射 + 章节知识点索引（§3.1）
5. **建 `检索协议.md`**：固化 9 条纪律（§3.3）
6. **（治理/风险/职业道德类考试）建 `IPPF索引.md`**：条款号→行锚点双源（§3.2）
7. **分批精讲**：A 块每批 2-3 章，writer + content + anchor 三 Agent，主循环核实（§4）
8. **错题库**：用户发题即入库，立即更新 `_汇总.md`（§5）
9. **每批结束跑验收 checklist**（§7）
10. **同步 CLAUDE.md**：科目目录路径、启动命令

> 本方法论 2026-08-09 由 CIA Part3 实战沉淀（A 块 20 章 + B 块 7 文件 60 考点全量完成，错题库 40+ 题），13 批教训全部收敛进第八节速查表。
