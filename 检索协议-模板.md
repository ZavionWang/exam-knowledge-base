# 检索协议模板（给 AI 读）

> 本文件是**通用模板**，不含任何具体考试的内容。
> 用法：复制到你的科目目录（如 `<考试名>/检索协议.md`），按 `START-HERE.md` 第 5 步实例化——把标注 `【实例化】` 的位置替换成你科目的大纲、教材和板块分布。

---

## 流程（检索纪律 ⚡）

0. **科目路由（多科目知识库必做）**：若知识库含多个科目目录，收到题目**第一动作是判定科目**——① 会话上下文（正在备考哪科）② 题号格式 ③ 题干框架词（IIA/IPPF vs ISACA/COBIT）④ **不确定就问用户一句**「这是哪科的题？」。判定后全部检索**限定本科目目录**（如 `--project <科目名>`），错题入库该科目的错题库，引用该科目的教材原文——**宁可问一句，不可跨科目引用**（跨科目可能导致答案来源、考点定位、错题收集全部错位）
1. **判断题型板块**：按本科目大纲的板块分布判断题目属于哪一块【实例化：列出板块占比，如"IT 类 40% → VI 板块"】
2. **先查 `_索引.md` 锁章节**：定位 **章 + 节**，绝不直接全目录扫描
3. **只 Grep 单文件**：锁定章节后，Grep 仅限对应章节文件，禁用全目录扫描（命中噪音×N，拖慢定位）
4. **并行读取**：已知节编号时可直接 `offset` 跳读，无需先搜；定位+读取尽量在同一条消息内并行发起
5. **做题输出默认完整四段式**（见下）
6. **本地教材兜底优先**：首选精讲笔记 → 教材原文 → 本地有确凿依据就下结论。**先本地、后外部**
7. **外部搜索设上限（含取证重活禁令）**：仅当本地教材无法定论（核对真题来源/选项争议）才外部搜索；**限 2 轮**，无果即放弃并如实标注"待确认"，不可反复死磕。**禁令**：外部取证只许读网页搜索摘要/页面正文——**禁止下载 PDF/文件、禁止写脚本解析网页**（"下载+解析"的耗时远超直接标注"待确认"）；本地盲区的题，答完在结尾标注：`⚠️ 本考点本地知识库未覆盖，答案基于外部检索/常识，建议补充精讲笔记`
8. **错题入库（强制约定，顺序在答案之后）**：用户发来的**每一道题都视为错题**（"凡是我发的题，都说明我不会"）→ **答案讲解先行，入库安排在公布答案之后**（用户需先看答案继续刷题，入库/汇总更新等事项一律排在答案输出之后）。入库 = 追加到 `错题库/错题记录.md`（单文件，每条一个 `## 错题 YYYY-MM-DD-NNN` section），格式见下；**同时更新 `错题库/_汇总.md`**（按章节/板块累计，薄弱点统计）

## 做题输出模式

### 完整四段式（默认）

- **输出顺序**：**答案先出**——四段式讲解作为第一块输出；错题入库、汇总更新等一切收尾工作放在答案之后，不得前置或并行拖延
- **考点定位**：`大纲板块 X → 章.节 知识点名`【实例化：如 `VI → 11.3 身份认证与加密`】
- **准则/大纲依据**：该知识点的理论来源，引用教材原句（标注 `文件:行`）
- **思路拆解**：解题逻辑，为什么选这个。**抓核心因果链，精炼表达**
- **干扰项解析**：其他选项为何错，考点陷阱、易混概念

## 错题库条目格式（单文件）

**全部错题存在 `错题库/错题记录.md`**（不拆多文件）。每道题一个 section，新题追加在文件顶部（倒序）。

```markdown
## 错题 YYYY-MM-DD-NNN — 主题
> 题号 xx/100，单选题。（发题即错题约定）

- **题目**：…（含选项）
- **正确答案：X**
- **错因分类**：概念混淆 / 准则不熟 / 计算失误 / 审题失误
- **知识点ID**：板块-章.节 知识点名【实例化：如 `VI-11.3 身份认证与加密`】
- **复盘**：考点解析 + ⭐陷阱 + 关联错题（编号）
```

> 知识点ID 格式：`板块罗马数字-章.节 知识点名`。每入库一题**立即更新 `错题库/_汇总.md`**（按板块统计薄弱点）。

## 精讲笔记格式

> 精讲笔记按大纲章节组织，每章一个文件（`精讲笔记/NN-章节名.md`），每个知识点一个条目，含：大纲位置 / 准则依据 / 核心结论 / 逻辑拆解 / 考法 / 易错点 / 关联考点。

```markdown
# 考点：<名称>
- 大纲位置：板块 X → 章.节 知识点名
- 准则依据：<引用教材/准则条款，如适用>
- 核心结论：…
- 易错点：…
- 关联考点：[[...]]
```

## 边界

- 知识库只回答 **本科目大纲内** 的内容；大纲外的题明说"不在考纲范围"
- OCR 原文可能有错字，引用时如明显异常，按常识纠正后标注
- 教材原文是基础层；**优先用精讲笔记**，笔记没有的再翻原文
