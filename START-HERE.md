# START-HERE · 开机引导（给 AI 读）

> 考生刚刚拿到本仓库，他的 AI 助手（你）的第一件事**不是讲题，是建档**。
> 本文件是建档的完整协议：问什么、按什么顺序做、每步产什么。

---

## 第一步：收集信息（一次问清，别啰嗦）

向考生确认：

1. **考什么考试？**（考公 / 考研 / CIA / CISA / CPA / CFA / 法考 / 软考 / PMP / 一建 / 教资 / 雅思…）
2. **教材是什么格式？**
   - 电子版 PDF（有文字层 / 扫描件）
   - Word / Markdown
   - 纸质书（需要拍照或扫描）
   - 还没有教材（只有考纲）
3. **教材存放在什么路径？**（如 `D:\教材\`、`~/Desktop/xxx`）
4. **有官方考纲 / 大纲吗？**（官方考试大纲 / 章节列表 / 官方说明文档，任意形式都行——`_索引.md` 生成和检索协议实例化都依赖它）

**然后做两件事验证（不要跳过）：**

- **检查教材路径确实存在且有文件**（如 `Test-Path D:\教材`）。不存在 → 请考生先建目录并把教材放进去，再继续
- **考纲必须到手**（考生没考纲 → 请他提供官方大纲或章节列表；连章节列表都没有 → 说明建档只能建到骨架，索引和协议要等考纲到位）

---

## 第二步：按答案建档（一步步来，每步说明在做什么）

1. **确定知识库根目录**（默认规则）：
   - **默认**：建在仓库克隆内，`<仓库根>/<考试名>/`（与示例实例 `CIA-Part3/` 平级），便于版本管理和同步
   - **可选**：考生已有自己的知识库目录（如 Obsidian Vault），可建在那边
   - **kb-search 工具注意**：方法论 §六 的 `kb-search` 默认扫描作者本机路径（Obsidian 考试知识库目录）——① 建在克隆内：把 kb-search 脚本的扫描根目录改为仓库根，或把新库目录加入扫描范围；② 不用 kb-search 的考生（AI 直接按检索协议 grep/查索引）无需任何配置
   - **Windows 中文路径注意**：仓库路径若含中文用户名，PowerShell 检索脚本可能乱码，见方法论 §6.3（脚本放纯 ASCII 路径）
2. **建科目目录 + 复制骨架**（目录统一口径，见下方"知识库目录结构"）：
   ```
   <考试名>/
   ├── _索引.md        # 从根目录「_索引-骨架模板.md」复制，第 3 步填充
   ├── 检索协议.md      # 从根目录「检索协议-模板.md」复制，第 5 步实例化
   ├── 精讲笔记/        # 空目录；可参考实例的 _README.md 但必须重写（清掉实例状态）
   ├── 错题库/          # 空目录；可参考实例的 _README.md 但必须重写（清掉实例状态）
   └── 教材原文/        # 第 4 步摄入教材时创建（⚠️ 版权内容，见下方版权说明）
   ```
3. **生成科目专属索引**：按考生提供的考纲，把 `_索引.md` 填充为「板块 → 章 → 节 → 知识点」四级映射——这是整个知识库的定位引擎
4. **摄入教材**：按《考试知识库-搭建方法论.md》执行——测文本层 →（扫描件走 OCR）→ 清洗 → 存进 `<考试名>/教材原文/`。**遇到文档里的示例路径，一律换成考生的真实路径**
5. **实例化检索协议**：把复制的 `检索协议-模板.md` 中的【实例化】位置替换为本科目内容：
   - 题型板块分布（按考纲的板块占比，**不许编造**，考纲没有就写"待考纲确认"）
   - 参考教材清单（考生自己的教材）
   - 大纲范围描述（"本知识库只回答 XXX 大纲内"）
   - 知识点 ID 示例（用本科目的真实章节编号）
   - 锚点层说明：IIA 体系类考试（如 CIA）才有 IPPF 条款索引层；**ISACA（CISA）等其他体系按各自的官方标准建**（如 ITAF/ISACA 标准），没有就删掉该层
   - 保留通用的部分：检索纪律、四段式输出、错题入库格式
6. **精讲策略（新库必读）**：新库精讲笔记为空，开讲初期走「教材原文兜底」模式（检索协议第 6 条）；**建议尽快按方法论 §四 分批精讲**（每批 2-3 章，写作+双审查 Agent）——精讲笔记是"最大杠杆"，先做精讲，而非堆原文。也可以边刷题边补精讲
7. **版权与 git 边界（重要）**：教材原文含版权内容：
   - 若根目录建在 git 克隆内，**把 `教材原文/` 加入 `.gitignore`**，防止 `git push` 泄露到公开仓库
   - 示例 `.gitignore`：`**/教材原文/`
8. **收尾**：告诉考生——"知识库已就绪：索引已建、教材已入库、协议已生效。现在可以开始发题了（初期答案以教材原文为依据，精讲笔记随刷题逐步补充）。"

---

## 知识库目录结构（统一口径）

```
<知识库根目录>/<考试名>/        ← 一场考试一个顶层目录
├── _索引.md                  ← ⭐四级定位引擎（板块→章→节→知识点→文件路径）
├── 检索协议.md                ← ⭐给 AI 的操作协议（检索纪律/四段式/错题入库）
├── 思维导图.md                ← 科目全景图（可选，人工复习用）
├── 教材原文/                  ← 基础层，OCR 产物，一章一文件（⚠️ 版权，勿推公开仓库）
├── 精讲笔记/                  ← ⭐精讲层，检索优先用它，笔记没有再翻原文
└── 错题库/                    ← 刷题闭环（错题记录.md + _汇总.md）
```

> 仓库根目录的 `CIA-Part3/` 就是这个结构的完整示例实例。**注意**：公开仓库中 `CIA-Part3/` 不含 `教材原文/`（版权排除），你本地建档时正常建即可。

---

## 第三步：日常开讲

此后每收到一道题，严格执行 `<考试名>/检索协议.md`：

```
查 _索引 定位（板块→章→节→知识点）→ 打开对应精讲/原文 → 四段式输出
（考点定位 → 依据 → 思路拆解 → 干扰项解析）→ 错题回写错题库（带知识点 ID）
```

---

## 边界

- 本仓库的 `CIA-Part3/` 只是**示例实例**，结构可复制，内容属于 CIA 考生
- 方法论文件里的命令、路径、脚本都是**作者实测环境的参考**，不是必须项；你的目标是让考生的知识库跑起来，不是复刻作者的环境
- 建库过程中考生提供的教材若含版权内容，默认留在考生本地，不要上传公开仓库
